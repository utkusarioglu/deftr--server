{
  "name": "@deftr/server-mock-data",
  "version": "1.0.0",
  "main": "lib/index.js",
  "license": "MIT",

  "scripts": {
    "build": "run-s clean build:tsc",
    "build:tsc": "tsc -b",
    "clean": "rimraf lib coverage tsconfig.tsbuildinfo",
    "start:watch": "tsc -w >/dev/null"
  },
  "dependencies": {
    "bcrypt": "^5.0.0",
    "dotenv": "^8.2.0"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "@types/dotenv": "^8.2.0",
    "typescript": "^4.0.2"
  }
}
