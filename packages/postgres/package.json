{
  "name": "@deftr/server-postgres",
  "version": "1.0.0",
  "main": "lib/index.js",
  "license": "MIT",
  "scripts": {
    "build": "run-s clean build:tsc",
    "build:tsc": "tsc -b",
    "clean": "rimraf lib coverage tsconfig.tsbuildinfo",
    "start": "node lib/index.js",
    "start:watch": "tsc -w >/dev/null"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "pg": "^8.5.1",
    "sequelize": "^6.3.5"
  },
  "devDependencies": {
    "@types/dotenv": "^8.2.0",
    "@types/pg": "^7.14.7"
  }
}
