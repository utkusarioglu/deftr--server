{
  "name": "@newspaper/server-auth",
  "version": "1.0.0",
  "main": "lib/index.js",
  "license": "MIT",
  "scripts": {
    "build": "run-s clean build:tsc",
    "build:tsc": "tsc -b",
    "clean": "rimraf lib coverage tsconfig.tsbuildinfo",
    "start:watch": "tsc -w"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
