{
  "name": "@newspaper/server-auth",
  "version": "1.0.0",
  "main": "lib/index.js",
  "license": "MIT",
  "scripts": {
    "build": "run-s clean build:tsc",
    "build:tsc": "tsc -b",
    "clean": "rimraf lib coverage tsconfig.tsbuildinfo",
    "start:watch": "tsc -w >/dev/null"
  },
  "devDependencies": {
    "@types/dotenv": "^8.2.0",
    "@types/express-session": "^1.17.3",
    "@types/passport": "^1.0.4",
    "@types/passport-local": "^1.0.33",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "bcrypt": "^5.0.0",
    "dotenv": "^8.2.0",
    "express-session": "^1.17.1",
    "passport": "^0.4.1",
    "passport-local": "^1.0.0"
  }
}
