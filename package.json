{
  "name": "@newspaper/server",
  "version": "1.0.0",
  "main": "lib/index.js",
  "author": "Utku Sarioglu <utkusarioglu@hotmail.com>",
  "license": "MIT",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "lerna": "^3.20.2",
    "nodemon": "^2.0.6",
    "npm-run-all": "^4.1.5",
    "typescript": "^3.7.5"
  },
  "scripts": {
    "build": "run-p clean build:each",
    "build:each": "lerna run build",
    "bootstrap": "lerna bootstrap",
    "clean": "lerna run --parallel clean",
    "release": "lerna publish",
    "test": "jest",
    "start:watch": "lerna run --parallel start:watch",
    "nodemon": "nodemon",
    "start:dev": "run-p start:watch nodemon"
  },
  "dependencies": {
    "rimraf": "^3.0.1"
  }
}
